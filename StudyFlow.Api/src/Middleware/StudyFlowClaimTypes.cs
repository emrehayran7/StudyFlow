namespace StudyFlow.Api.src.Middleware
{
    public static class StudyFlowClaimTypes
    {
        public const string RequestId = "studyflow:request_id";
        public const string SessionId = "studyflow:session_id";
        public const string UserId = "studyflow:user_id";
    }
}
